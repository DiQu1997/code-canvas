{
  "meta": {
    "title": "nano-vllm 推理引擎",
    "subtitle": "github.com/GeeeekExplorer/nano-vllm · ~1200 行的 vLLM 教学重写",
    "audience": "有后端经验、想读懂推理引擎内部机制的工程师；中文；准确优先，类比点到为止"
  },
  "regions": [
    {
      "id": "A",
      "title": "故事线 A · 引擎循环",
      "blurb": "每个 step：调度 → 前向 → 采样 → 写回",
      "hue": "blue",
      "cards": [
        "step",
        "run",
        "sampler"
      ]
    },
    {
      "id": "B",
      "title": "故事线 B · 调度决策",
      "blurb": "先喂新客成 prefill 批，收不到才续老客",
      "hue": "violet",
      "cards": [
        "schedule",
        "preempt",
        "postprocess"
      ]
    },
    {
      "id": "C",
      "title": "故事线 C · 显存账本",
      "blurb": "KV cache 分块记账，相同前缀按哈希复用",
      "hue": "green",
      "cards": [
        "can_allocate",
        "allocate",
        "hash_blocks"
      ]
    }
  ],
  "cards": [
    {
      "id": "step",
      "name": "LLMEngine.step()",
      "file": "engine/llm_engine.py:49",
      "lang": "py",
      "layout": {
        "col": 0,
        "band": 0
      },
      "collapsed": false,
      "code": "def step(self):\n    seqs, is_prefill = self.scheduler.schedule()\n    num_tokens = sum(seq.num_scheduled_tokens for seq in seqs) if is_prefill else -len(seqs)\n    token_ids = self.model_runner.call(\"run\", seqs, is_prefill)\n    self.scheduler.postprocess(seqs, token_ids, is_prefill)\n    outputs = [(seq.seq_id, seq.completion_token_ids) for seq in seqs if seq.is_finished]\n    return outputs, num_tokens",
      "terms": [
        {
          "line": 2,
          "token": "is_prefill",
          "note": "整批同型标志：一轮要么全 prefill 要么全 decode，attention 按它切换 varlen / paged 两条路径。"
        }
      ]
    },
    {
      "id": "run",
      "name": "ModelRunner.run()",
      "file": "engine/model_runner.py:214",
      "lang": "py",
      "layout": {
        "col": 0,
        "band": 2
      },
      "collapsed": true,
      "code": "def run(self, seqs: list[Sequence], is_prefill: bool) -> list[int]:\n    input_ids, positions = self.prepare_prefill(seqs) if is_prefill else self.prepare_decode(seqs)\n    temperatures = self.prepare_sample(seqs) if self.rank == 0 else None\n    logits = self.run_model(input_ids, positions, is_prefill)\n    token_ids = self.sampler(logits, temperatures).tolist() if self.rank == 0 else None\n    reset_context()\n    return token_ids"
    },
    {
      "id": "sampler",
      "name": "Sampler.forward()",
      "file": "layers/sampler.py:7",
      "lang": "py",
      "layout": {
        "col": 0,
        "band": 3
      },
      "collapsed": true,
      "code": "@torch.compile\ndef forward(self, logits: torch.Tensor, temperatures: torch.Tensor):\n    logits = logits.float().div_(temperatures.unsqueeze(dim=1))\n    probs = torch.softmax(logits, dim=-1)\n    sample_tokens = probs.div_(torch.empty_like(probs).exponential_(1).clamp_min_(1e-10)).argmax(dim=-1)\n    return sample_tokens"
    },
    {
      "id": "schedule",
      "name": "Scheduler.schedule()",
      "file": "engine/scheduler.py:25",
      "lang": "py",
      "layout": {
        "col": 1,
        "band": 0
      },
      "collapsed": false,
      "code": "def schedule(self) -> tuple[list[Sequence], bool]:\n    scheduled_seqs = []\n    num_batched_tokens = 0\n\n    # prefill\n    while self.waiting and len(scheduled_seqs) < self.max_num_seqs:\n        seq = self.waiting[0]\n        remaining = self.max_num_batched_tokens - num_batched_tokens\n        if remaining == 0:\n            break\n        if not seq.block_table:\n            num_cached_blocks = self.block_manager.can_allocate(seq)\n            if num_cached_blocks == -1:\n                break\n            num_tokens = seq.num_tokens - num_cached_blocks * self.block_size\n        else:\n            num_tokens = seq.num_tokens - seq.num_cached_tokens\n        if remaining < num_tokens and scheduled_seqs:  # only allow chunked prefill for the first seq\n            break\n        if not seq.block_table:\n            self.block_manager.allocate(seq, num_cached_blocks)\n        seq.num_scheduled_tokens = min(num_tokens, remaining)\n        num_batched_tokens += seq.num_scheduled_tokens\n        if seq.num_cached_tokens + seq.num_scheduled_tokens == seq.num_tokens:\n            seq.status = SequenceStatus.RUNNING\n            self.waiting.popleft()\n            self.running.append(seq)\n        scheduled_seqs.append(seq)\n\n    if scheduled_seqs:\n        return scheduled_seqs, True\n\n    # decode\n    while self.running and len(scheduled_seqs) < self.max_num_seqs:\n        seq = self.running.popleft()\n        while not self.block_manager.can_append(seq):\n            if self.running:\n                self.preempt(self.running.pop())\n            else:\n                self.preempt(seq)\n                break\n        else:\n            seq.num_scheduled_tokens = 1\n            seq.is_prefill = False\n            self.block_manager.may_append(seq)\n            scheduled_seqs.append(seq)\n    assert scheduled_seqs\n    self.running.extendleft(reversed(scheduled_seqs))\n    return scheduled_seqs, False",
      "blocks": [
        {
          "name": "Prefill 批次",
          "summary": "waiting 队头能塞就塞，直到 token 预算用完",
          "lines": [
            5,
            31
          ],
          "children": [
            {
              "name": "前缀缓存探测",
              "summary": "先问账本命中几块，只调度增量部分",
              "lines": [
                11,
                17
              ],
              "explain": "can_allocate 返回的不是布尔而是「命中了几块」，-1 表示装不下。它只探测不落账——真正把块挂到 seq 上的是 allocate。探测与分配分离，预算判断失败时才能无副作用地退出循环。"
            }
          ],
          "explain": "为什么 prefill 绝对优先？新请求的首 token 延迟对体验影响最大，而且 prefill 一次能吃进大量 token、GPU 利用率最高。只要 waiting 里还有装得下的请求就先不做 decode——这是吞吐与首字延迟之间的取舍，nano-vllm 选了最简单的一边。"
        },
        {
          "name": "Decode 批次",
          "summary": "running 逐条续 1 个 token；不够块就抢占",
          "lines": [
            33,
            46
          ],
          "children": [
            {
              "name": "抢占腾块",
              "summary": "先牺牲队尾；实在不行牺牲自己",
              "lines": [
                36,
                41
              ],
              "explain": "can_append 只在「下一个 token 恰好开新块」时才需要空块。没有空块：从队尾 pop 一条抢占（块全部回收、打回 waiting），最坏情况牺牲的是自己——此时 break 掉，本条不进批。"
            }
          ],
          "explain": "一条新请求都收不进来才轮到 decode：每条 running seq 只续 1 个 token。注意 while…else 的用法：只有内层抢占循环正常结束（没有 break）才会调度这条 seq。"
        },
        {
          "name": "收尾",
          "summary": "断言非空 + 恢复队序",
          "lines": [
            47,
            49
          ],
          "explain": "assert 兜底：decode 分支至少要产出一条，否则说明块账本泄漏。extendleft(reversed(…)) 把这批放回队头、保持原顺序，下一轮还是它们先跑。"
        }
      ],
      "terms": [
        {
          "line": 3,
          "token": "num_batched_tokens",
          "note": "本批已占用的 token 预算，对着 max_num_batched_tokens 卡上限——prefill 批的大小由它决定。"
        },
        {
          "line": 22,
          "token": "num_scheduled_tokens",
          "note": "这条 seq 本轮真正要算的 token 数；chunked prefill 时小于剩余全量，decode 时恒为 1。"
        }
      ]
    },
    {
      "id": "preempt",
      "name": "Scheduler.preempt()",
      "file": "engine/scheduler.py:75",
      "lang": "py",
      "layout": {
        "col": 1,
        "band": 2
      },
      "collapsed": true,
      "code": "def preempt(self, seq: Sequence):\n    seq.status = SequenceStatus.WAITING\n    seq.is_prefill = True\n    self.block_manager.deallocate(seq)\n    self.waiting.appendleft(seq)"
    },
    {
      "id": "postprocess",
      "name": "Scheduler.postprocess()",
      "file": "engine/scheduler.py:81",
      "lang": "py",
      "layout": {
        "col": 1,
        "band": 3
      },
      "collapsed": true,
      "code": "def postprocess(self, seqs: list[Sequence], token_ids: list[int], is_prefill: bool):\n    for seq, token_id in zip(seqs, token_ids):\n        self.block_manager.hash_blocks(seq)\n        seq.num_cached_tokens += seq.num_scheduled_tokens\n        seq.num_scheduled_tokens = 0\n        if is_prefill and seq.num_cached_tokens < seq.num_tokens:\n            continue\n        seq.append_token(token_id)\n        if (not seq.ignore_eos and token_id == self.eos) or seq.num_completion_tokens == seq.max_tokens:\n            seq.status = SequenceStatus.FINISHED\n            self.block_manager.deallocate(seq)\n            self.running.remove(seq)",
      "terms": [
        {
          "line": 4,
          "token": "num_cached_tokens",
          "note": "已写入 KV cache 的 token 数；prefill 分几轮时靠它记录进度，也是下次增量的起点。"
        }
      ]
    },
    {
      "id": "can_allocate",
      "name": "BlockManager.can_allocate()",
      "file": "engine/block_manager.py:58",
      "lang": "py",
      "layout": {
        "col": 2,
        "band": 0
      },
      "collapsed": true,
      "code": "def can_allocate(self, seq: Sequence) -> int:\n    h = -1\n    num_cached_blocks = 0\n    num_new_blocks = seq.num_blocks\n    for i in range(seq.num_blocks - 1):\n        token_ids = seq.block(i)\n        h = self.compute_hash(token_ids, h)\n        block_id = self.hash_to_block_id.get(h, -1)\n        if block_id == -1 or self.blocks[block_id].token_ids != token_ids:\n            break\n        num_cached_blocks += 1\n        if block_id in self.used_block_ids:\n            num_new_blocks -= 1\n    if len(self.free_block_ids) < num_new_blocks:\n        return -1\n    return num_cached_blocks",
      "terms": [
        {
          "line": 4,
          "token": "num_new_blocks",
          "note": "按全新分配估算的块数；每命中一个「在用」的共享块就减一，用来判断 free 池够不够。"
        }
      ]
    },
    {
      "id": "allocate",
      "name": "BlockManager.allocate()",
      "file": "engine/block_manager.py:75",
      "lang": "py",
      "layout": {
        "col": 2,
        "band": 2
      },
      "collapsed": true,
      "code": "def allocate(self, seq: Sequence, num_cached_blocks: int):\n    assert not seq.block_table\n    h = -1\n    for i in range(num_cached_blocks):\n        token_ids = seq.block(i)\n        h = self.compute_hash(token_ids, h)\n        block_id = self.hash_to_block_id[h]\n        block = self.blocks[block_id]\n        if block_id in self.used_block_ids:\n            block.ref_count += 1\n        else:\n            block.ref_count = 1\n            self.free_block_ids.remove(block_id)\n            self.used_block_ids.add(block_id)\n        seq.block_table.append(block_id)\n    for i in range(num_cached_blocks, seq.num_blocks):\n        seq.block_table.append(self._allocate_block())\n    seq.num_cached_tokens = num_cached_blocks * self.block_size",
      "blocks": [
        {
          "name": "认领命中块",
          "summary": "缓存命中的块挂进 block_table，引用计数 +1",
          "lines": [
            4,
            15
          ],
          "explain": "沿同一条哈希链重放认领：块可能正被别的 seq 用着（ref_count +1 共享），也可能已回到 free 队列但内容未被覆盖（从 free 里捞回来复活）。前缀缓存不需要独立存储——它就住在 KV cache 块里。"
        },
        {
          "name": "增量新块",
          "summary": "缓存没有的部分拿全新块",
          "lines": [
            16,
            18
          ],
          "explain": "缓存没覆盖的尾部照常拿新块。num_cached_tokens 按整块计，最后一个不满的块永远要重算。"
        }
      ],
      "terms": [
        {
          "line": 15,
          "token": "block_table",
          "note": "seq 的页表：逻辑块序号 → 物理块 id。attention 用它把逻辑位置翻译成显存里的块。"
        }
      ]
    },
    {
      "id": "hash_blocks",
      "name": "BlockManager.hash_blocks()",
      "file": "engine/block_manager.py:110",
      "lang": "py",
      "layout": {
        "col": 2,
        "band": 3
      },
      "collapsed": true,
      "code": "def hash_blocks(self, seq: Sequence):\n    start = seq.num_cached_tokens // self.block_size\n    end = (seq.num_cached_tokens + seq.num_scheduled_tokens) // self.block_size\n    if start == end: return\n    h = self.blocks[seq.block_table[start - 1]].hash if start > 0 else -1\n    for i in range(start, end):\n        block = self.blocks[seq.block_table[i]]\n        token_ids = seq.block(i)\n        h = self.compute_hash(token_ids, h)\n        block.update(h, token_ids)\n        self.hash_to_block_id[h] = block.block_id"
    },
    {
      "id": "kv-pool",
      "kind": "state",
      "name": "KV 块池 · 实例快照",
      "file": "engine/block_manager.py",
      "lang": "py",
      "layout": {
        "col": 3,
        "band": 1
      },
      "collapsed": false,
      "nodes": [
        {
          "id": "fq",
          "kind": "array",
          "label": "free_block_queue（空闲块）"
        },
        {
          "id": "btA",
          "kind": "array",
          "label": "SeqA.block_table"
        },
        {
          "id": "btB",
          "kind": "array",
          "label": "SeqB.block_table"
        },
        {
          "id": "b7",
          "kind": "record",
          "label": "Block 7（系统提示词前缀）"
        },
        {
          "id": "h2b",
          "kind": "map",
          "label": "hash_to_block_id（前缀账本）"
        }
      ],
      "states": [
        {
          "step": 1,
          "nodes": {
            "fq": {
              "cells": [
                2,
                5,
                8,
                9
              ]
            },
            "btA": {
              "cells": [
                7,
                1,
                4
              ]
            },
            "btB": {
              "cells": []
            },
            "b7": {
              "rows": [
                [
                  "ref_count",
                  1
                ],
                [
                  "hash",
                  "h(sys)"
                ]
              ]
            },
            "h2b": {
              "rows": [
                [
                  "h(sys)",
                  7
                ],
                [
                  "h(sys+a1)",
                  1
                ]
              ]
            }
          }
        },
        {
          "step": 3,
          "note": "SeqB 入场：前缀命中 7 号块直接复用（ref+1），只为增量借了 2 号块",
          "nodes": {
            "fq": {
              "cells": [
                5,
                8,
                9
              ]
            },
            "btA": {
              "cells": [
                7,
                1,
                4
              ]
            },
            "btB": {
              "cells": [
                7,
                2
              ]
            },
            "b7": {
              "rows": [
                [
                  "ref_count",
                  2
                ],
                [
                  "hash",
                  "h(sys)"
                ]
              ]
            },
            "h2b": {
              "rows": [
                [
                  "h(sys)",
                  7
                ],
                [
                  "h(sys+a1)",
                  1
                ]
              ]
            }
          }
        },
        {
          "step": 4,
          "note": "抢占 SeqB：2 号块归还队尾，7 号块 ref−1——共享块不清空，账本不动",
          "nodes": {
            "fq": {
              "cells": [
                5,
                8,
                9,
                2
              ]
            },
            "btA": {
              "cells": [
                7,
                1,
                4
              ]
            },
            "btB": {
              "cells": []
            },
            "b7": {
              "rows": [
                [
                  "ref_count",
                  1
                ],
                [
                  "hash",
                  "h(sys)"
                ]
              ]
            },
            "h2b": {
              "rows": [
                [
                  "h(sys)",
                  7
                ],
                [
                  "h(sys+a1)",
                  1
                ]
              ]
            }
          }
        }
      ]
    }
  ],
  "wires": [
    {
      "id": "w-sched",
      "kind": "call",
      "from": {
        "card": "step",
        "line": 2
      },
      "to": {
        "card": "schedule"
      }
    },
    {
      "id": "w-run",
      "kind": "call",
      "from": {
        "card": "step",
        "line": 4,
        "side": "left"
      },
      "to": {
        "card": "run",
        "side": "left"
      }
    },
    {
      "id": "w-post",
      "kind": "call",
      "from": {
        "card": "step",
        "line": 5
      },
      "to": {
        "card": "postprocess"
      }
    },
    {
      "id": "w-sampler",
      "kind": "call",
      "from": {
        "card": "run",
        "line": 5,
        "side": "left"
      },
      "to": {
        "card": "sampler",
        "side": "left"
      }
    },
    {
      "id": "w-canalloc",
      "kind": "call",
      "from": {
        "card": "schedule",
        "line": 12
      },
      "to": {
        "card": "can_allocate"
      }
    },
    {
      "id": "w-alloc",
      "kind": "call",
      "from": {
        "card": "schedule",
        "line": 21
      },
      "to": {
        "card": "allocate"
      }
    },
    {
      "id": "w-preempt",
      "kind": "call",
      "from": {
        "card": "schedule",
        "line": 38,
        "side": "left"
      },
      "to": {
        "card": "preempt",
        "side": "left"
      }
    },
    {
      "id": "w-hash",
      "kind": "call",
      "from": {
        "card": "postprocess",
        "line": 3
      },
      "to": {
        "card": "hash_blocks"
      }
    },
    {
      "id": "d-token",
      "kind": "data",
      "label": "token_ids",
      "from": {
        "card": "run",
        "line": 5
      },
      "to": {
        "card": "postprocess"
      }
    },
    {
      "id": "d-cached",
      "kind": "data",
      "label": "num_cached_blocks",
      "from": {
        "card": "can_allocate",
        "line": 16,
        "side": "left"
      },
      "to": {
        "card": "allocate",
        "side": "left"
      }
    },
    {
      "id": "s-pool",
      "kind": "struct",
      "label": "按 hash 索引",
      "from": {
        "card": "allocate"
      },
      "to": {
        "card": "kv-pool"
      }
    }
  ],
  "notes": [
    {
      "id": "bg0",
      "flavor": "bg",
      "tag": "背景 · 这是什么",
      "text": "vLLM 的 ~1200 行教学重写。三条线：引擎循环怎么转、调度器怎么决定谁跑、KV cache 显存怎么记账。模型本体（qwen3.py）不在图内——这张图只讲推理引擎。",
      "place": {
        "corner": "nw"
      },
      "step": 0
    },
    {
      "id": "bg-paged",
      "flavor": "bg",
      "tag": "背景 · PagedAttention",
      "text": "KV cache 按固定大小的块分页存放，seq 只持有 block_table（页表）。相同前缀的块可被多条 seq 共享——这是省显存的核心来源。",
      "anchor": {
        "card": "can_allocate",
        "line": 5
      },
      "place": {
        "side": "above",
        "of": "can_allocate"
      },
      "step": 3
    },
    {
      "id": "n-chunk",
      "flavor": "intent",
      "tag": "NOTE · scheduler.py:42",
      "text": "只允许批里第一条 seq 做 chunked prefill：长 prompt 不会饿死，批的形状也保持简单。",
      "anchor": {
        "card": "schedule",
        "line": 18
      },
      "place": {
        "side": "left",
        "of": "schedule"
      },
      "step": 2
    },
    {
      "id": "n-preempt",
      "flavor": "intent",
      "tag": "NOTE · scheduler.py:60",
      "text": "抢占 = 块全退回、状态打回 WAITING、下次重新 prefill。代价是重算，换来的是 decode 批永远能推进。",
      "anchor": {
        "card": "schedule",
        "line": 38
      },
      "place": {
        "side": "left",
        "of": "schedule"
      },
      "step": 4
    },
    {
      "id": "n-gumbel",
      "flavor": "intent",
      "tag": "NOTE · sampler.py:11",
      "text": "除以指数噪声再 argmax = Gumbel-max 技巧：一行完成按概率采样，全程留在 GPU。",
      "anchor": {
        "card": "sampler",
        "line": 5
      },
      "place": {
        "side": "left",
        "of": "sampler"
      },
      "step": 5
    }
  ],
  "steps": [
    {
      "title": "总览",
      "caption": "nano-vllm 推理引擎全景：左列引擎循环、中列调度决策、右列显存账本。灰线 = 调用，琥珀线 = 数据流。拖拽缩放自由看，或 ▶ 沿故事线走。",
      "fit": true
    },
    {
      "title": "① 一次 step 的骨架",
      "caption": "每个 step：调度器挑一批 seq → 模型跑一步前向 → 后处理写回。批要么全 prefill 要么全 decode，is_prefill 一路随行决定每一层的行为。",
      "wires": [
        "w-sched",
        "w-run",
        "w-post",
        "d-token"
      ],
      "lines": [
        [
          "step",
          2
        ],
        [
          "step",
          4
        ],
        [
          "step",
          5
        ]
      ],
      "focus": [
        "step",
        "schedule",
        "run",
        "postprocess"
      ],
      "storyline": "A"
    },
    {
      "title": "② 调度：大纲即策略",
      "caption": "schedule 的三段大纲就是策略本身：先尽量收新请求组成 prefill 批；一条都收不到才轮到 decode 批；结尾恢复队序保证公平。点色条看每段细节。",
      "wires": [
        "w-sched"
      ],
      "focus": [
        "schedule",
        "n-chunk",
        "step"
      ],
      "storyline": "B"
    },
    {
      "title": "③ 显存账本与前缀缓存",
      "caption": "prefill 前先探账本：逐块算链式哈希，命中的块直接认领（引用计数 +1），只为增量拿新块。num_cached_blocks 从探测流向分配——省下的就是不用重算的。",
      "wires": [
        "w-canalloc",
        "w-alloc",
        "d-cached",
        "s-pool"
      ],
      "unfold": [
        [
          "schedule",
          "Prefill 批次"
        ],
        [
          "schedule",
          "前缀缓存探测"
        ],
        [
          "allocate",
          "认领命中块"
        ]
      ],
      "expand": [
        "can_allocate",
        "allocate"
      ],
      "lines": [
        [
          "schedule",
          12
        ],
        [
          "schedule",
          21
        ]
      ],
      "focus": [
        "schedule",
        "can_allocate",
        "allocate",
        "kv-pool"
      ],
      "storyline": "C"
    },
    {
      "title": "④ 不够块就抢占",
      "caption": "decode 每条只要 1 个新 token，但可能正好需要 1 个新块。没有空块时：牺牲 running 队尾去重算，保住批次推进；只剩自己时把自己也打回 waiting。",
      "wires": [
        "w-preempt"
      ],
      "unfold": [
        [
          "schedule",
          "Decode 批次"
        ],
        [
          "schedule",
          "抢占腾块"
        ]
      ],
      "expand": [
        "preempt"
      ],
      "lines": [
        [
          "schedule",
          38
        ],
        [
          "schedule",
          40
        ]
      ],
      "focus": [
        "schedule",
        "preempt",
        "n-preempt",
        "kv-pool"
      ],
      "storyline": "B"
    },
    {
      "title": "⑤ token 的回程",
      "caption": "sampler 用 Gumbel-max 一行采样；token_ids 回到 postprocess：append 进 seq、写满整块就登记哈希供后来者复用、命中 EOS 或超长就归还全部块。",
      "wires": [
        "w-sampler",
        "d-token",
        "w-hash"
      ],
      "expand": [
        "run",
        "sampler",
        "postprocess"
      ],
      "lines": [
        [
          "postprocess",
          8
        ],
        [
          "postprocess",
          9
        ]
      ],
      "focus": [
        "run",
        "sampler",
        "postprocess",
        "n-gumbel"
      ],
      "storyline": "A"
    }
  ],
  "files": {
    "engine/llm_engine.py": "import atexit\nfrom dataclasses import fields\nfrom time import perf_counter\nfrom tqdm.auto import tqdm\nfrom transformers import AutoTokenizer\nimport torch.multiprocessing as mp\n\nfrom nanovllm.config import Config\nfrom nanovllm.sampling_params import SamplingParams\nfrom nanovllm.engine.sequence import Sequence\nfrom nanovllm.engine.scheduler import Scheduler\nfrom nanovllm.engine.model_runner import ModelRunner\n\n\nclass LLMEngine:\n\n    def __init__(self, model, **kwargs):\n        config_fields = {field.name for field in fields(Config)}\n        config_kwargs = {k: v for k, v in kwargs.items() if k in config_fields}\n        config = Config(model, **config_kwargs)\n        Sequence.block_size = config.kvcache_block_size\n        self.ps = []\n        self.events = []\n        ctx = mp.get_context(\"spawn\")\n        for i in range(1, config.tensor_parallel_size):\n            event = ctx.Event()\n            process = ctx.Process(target=ModelRunner, args=(config, i, event))\n            process.start()\n            self.ps.append(process)\n            self.events.append(event)\n        self.model_runner = ModelRunner(config, 0, self.events)\n        self.tokenizer = AutoTokenizer.from_pretrained(config.model, use_fast=True)\n        config.eos = self.tokenizer.eos_token_id\n        self.scheduler = Scheduler(config)\n        atexit.register(self.exit)\n\n    def exit(self):\n        self.model_runner.call(\"exit\")\n        del self.model_runner\n        for p in self.ps:\n            p.join()\n\n    def add_request(self, prompt: str | list[int], sampling_params: SamplingParams):\n        if isinstance(prompt, str):\n            prompt = self.tokenizer.encode(prompt)\n        seq = Sequence(prompt, sampling_params)\n        self.scheduler.add(seq)\n\n    def step(self):\n        seqs, is_prefill = self.scheduler.schedule()\n        num_tokens = sum(seq.num_scheduled_tokens for seq in seqs) if is_prefill else -len(seqs)\n        token_ids = self.model_runner.call(\"run\", seqs, is_prefill)\n        self.scheduler.postprocess(seqs, token_ids, is_prefill)\n        outputs = [(seq.seq_id, seq.completion_token_ids) for seq in seqs if seq.is_finished]\n        return outputs, num_tokens\n\n    def is_finished(self):\n        return self.scheduler.is_finished()\n\n    def generate(\n        self,\n        prompts: list[str] | list[list[int]],\n        sampling_params: SamplingParams | list[SamplingParams],\n        use_tqdm: bool = True,\n    ) -> list[str]:\n        pbar = tqdm(total=len(prompts), desc=\"Generating\", dynamic_ncols=True, disable=not use_tqdm)\n        if not isinstance(sampling_params, list):\n            sampling_params = [sampling_params] * len(prompts)\n        for prompt, sp in zip(prompts, sampling_params):\n            self.add_request(prompt, sp)\n        outputs = {}\n        prefill_throughput = decode_throughput = 0.\n        while not self.is_finished():\n            t = perf_counter()\n            output, num_tokens = self.step()\n            if num_tokens > 0:\n                prefill_throughput = num_tokens / (perf_counter() - t)\n            else:\n                decode_throughput = -num_tokens / (perf_counter() - t)\n            pbar.set_postfix({\n                \"Prefill\": f\"{int(prefill_throughput)}tok/s\",\n                \"Decode\": f\"{int(decode_throughput)}tok/s\",\n            })\n            for seq_id, token_ids in output:\n                outputs[seq_id] = token_ids\n                pbar.update(1)\n        pbar.close()\n        outputs = [outputs[seq_id] for seq_id in sorted(outputs.keys())]\n        outputs = [{\"text\": self.tokenizer.decode(token_ids), \"token_ids\": token_ids} for token_ids in outputs]\n        return outputs\n",
    "engine/model_runner.py": "import pickle\nimport torch\nimport torch.distributed as dist\nfrom multiprocessing.synchronize import Event\nfrom multiprocessing.shared_memory import SharedMemory\n\nfrom nanovllm.config import Config\nfrom nanovllm.engine.sequence import Sequence\nfrom nanovllm.models.qwen3 import Qwen3ForCausalLM\nfrom nanovllm.layers.sampler import Sampler\nfrom nanovllm.utils.context import set_context, get_context, reset_context\nfrom nanovllm.utils.loader import load_model\n\n\nclass ModelRunner:\n\n    def __init__(self, config: Config, rank: int, event: Event | list[Event]):\n        self.config = config\n        hf_config = config.hf_config\n        self.block_size = config.kvcache_block_size\n        self.enforce_eager = config.enforce_eager\n        self.world_size = config.tensor_parallel_size\n        self.rank = rank\n        self.event = event\n\n        dist.init_process_group(\"nccl\", \"tcp://localhost:2333\", world_size=self.world_size, rank=rank)\n        torch.cuda.set_device(rank)\n        default_dtype = torch.get_default_dtype()\n        torch.set_default_dtype(hf_config.dtype)\n        torch.set_default_device(\"cuda\")\n        self.model = Qwen3ForCausalLM(hf_config)\n        load_model(self.model, config.model)\n        self.sampler = Sampler()\n        self.warmup_model()\n        self.allocate_kv_cache()\n        if not self.enforce_eager:\n            self.capture_cudagraph()\n        torch.set_default_device(\"cpu\")\n        torch.set_default_dtype(default_dtype)\n\n        if self.world_size > 1:\n            if rank == 0:\n                self.shm = SharedMemory(name=\"nanovllm\", create=True, size=2**20)\n                dist.barrier()\n            else:\n                dist.barrier()\n                self.shm = SharedMemory(name=\"nanovllm\")\n                self.loop()\n\n    def exit(self):\n        if self.world_size > 1:\n            self.shm.close()\n            dist.barrier()\n            if self.rank == 0:\n                self.shm.unlink()\n        if not self.enforce_eager:\n            del self.graphs, self.graph_pool\n        torch.cuda.synchronize()\n        dist.destroy_process_group()\n\n    def loop(self):\n        while True:\n            method_name, args = self.read_shm()\n            self.call(method_name, *args)\n            if method_name == \"exit\":\n                break\n\n    def read_shm(self):\n        assert self.world_size > 1 and self.rank > 0\n        self.event.wait()\n        n = int.from_bytes(self.shm.buf[0:4], \"little\")\n        method_name, *args = pickle.loads(self.shm.buf[4:n+4])\n        self.event.clear()\n        return method_name, args\n\n    def write_shm(self, method_name, *args):\n        assert self.world_size > 1 and self.rank == 0\n        data = pickle.dumps([method_name, *args])\n        n = len(data)\n        self.shm.buf[0:4] = n.to_bytes(4, \"little\")\n        self.shm.buf[4:n+4] = data\n        for event in self.event:\n            event.set()\n\n    def call(self, method_name, *args):\n        if self.world_size > 1 and self.rank == 0:\n            self.write_shm(method_name, *args)\n        method = getattr(self, method_name, None)\n        return method(*args)\n\n    def warmup_model(self):\n        torch.cuda.empty_cache()\n        torch.cuda.reset_peak_memory_stats()\n        max_num_batched_tokens, max_model_len = self.config.max_num_batched_tokens, self.config.max_model_len\n        seq_len = min(max_num_batched_tokens, max_model_len)\n        num_seqs = min(max_num_batched_tokens // seq_len, self.config.max_num_seqs)\n        seqs = [Sequence([0] * seq_len) for _ in range(num_seqs)]\n        for seq in seqs:\n            seq.num_scheduled_tokens = seq_len\n        self.run(seqs, True)\n        torch.cuda.empty_cache()\n\n    def allocate_kv_cache(self):\n        config = self.config\n        hf_config = config.hf_config\n        free, total = torch.cuda.mem_get_info()\n        used = total - free\n        peak = torch.cuda.memory_stats()[\"allocated_bytes.all.peak\"]\n        current = torch.cuda.memory_stats()[\"allocated_bytes.all.current\"]\n        num_kv_heads = hf_config.num_key_value_heads // self.world_size\n        head_dim = getattr(hf_config, \"head_dim\", hf_config.hidden_size // hf_config.num_attention_heads)\n        block_bytes = 2 * hf_config.num_hidden_layers * self.block_size * num_kv_heads * head_dim * hf_config.dtype.itemsize\n        config.num_kvcache_blocks = int(total * config.gpu_memory_utilization - used - peak + current) // block_bytes\n        assert config.num_kvcache_blocks > 0\n        self.kv_cache = torch.empty(2, hf_config.num_hidden_layers, config.num_kvcache_blocks, self.block_size, num_kv_heads, head_dim)\n        layer_id = 0\n        for module in self.model.modules():\n            if hasattr(module, \"k_cache\") and hasattr(module, \"v_cache\"):\n                module.k_cache = self.kv_cache[0, layer_id]\n                module.v_cache = self.kv_cache[1, layer_id]\n                layer_id += 1\n\n    def prepare_block_tables(self, seqs: list[Sequence]):\n        max_len = max(len(seq.block_table) for seq in seqs)\n        block_tables = [seq.block_table + [-1] * (max_len - len(seq.block_table)) for seq in seqs]\n        block_tables = torch.tensor(block_tables, dtype=torch.int32, pin_memory=True).cuda(non_blocking=True)\n        return block_tables\n\n    def prepare_prefill(self, seqs: list[Sequence]):\n        input_ids = []\n        positions = []\n        cu_seqlens_q = [0]\n        cu_seqlens_k = [0]\n        max_seqlen_q = 0\n        max_seqlen_k = 0\n        slot_mapping = []\n        block_tables = None\n        for seq in seqs:\n            start = seq.num_cached_tokens\n            seqlen_q = seq.num_scheduled_tokens\n            end = start + seqlen_q\n            seqlen_k = end\n            input_ids.extend(seq[start:end])\n            positions.extend(range(start, end))\n            cu_seqlens_q.append(cu_seqlens_q[-1] + seqlen_q)\n            cu_seqlens_k.append(cu_seqlens_k[-1] + seqlen_k)\n            max_seqlen_q = max(seqlen_q, max_seqlen_q)\n            max_seqlen_k = max(seqlen_k, max_seqlen_k)\n            if not seq.block_table:    # warmup\n                continue\n            start_block = start // self.block_size\n            end_block = (end + self.block_size - 1) // self.block_size\n            for i in range(start_block, end_block):\n                slot_start = seq.block_table[i] * self.block_size\n                if i == start_block:\n                    slot_start += start % self.block_size\n                if i != end_block - 1:\n                    slot_end = seq.block_table[i] * self.block_size + self.block_size\n                else:\n                    slot_end = seq.block_table[i] * self.block_size + end - i * self.block_size\n                slot_mapping.extend(range(slot_start, slot_end))\n        if cu_seqlens_k[-1] > cu_seqlens_q[-1]:    # prefix cache\n            block_tables = self.prepare_block_tables(seqs)\n        input_ids = torch.tensor(input_ids, dtype=torch.int64, pin_memory=True).cuda(non_blocking=True)\n        positions = torch.tensor(positions, dtype=torch.int64, pin_memory=True).cuda(non_blocking=True)\n        cu_seqlens_q = torch.tensor(cu_seqlens_q, dtype=torch.int32, pin_memory=True).cuda(non_blocking=True)\n        cu_seqlens_k = torch.tensor(cu_seqlens_k, dtype=torch.int32, pin_memory=True).cuda(non_blocking=True)\n        slot_mapping = torch.tensor(slot_mapping, dtype=torch.int32, pin_memory=True).cuda(non_blocking=True)\n        set_context(True, cu_seqlens_q, cu_seqlens_k, max_seqlen_q, max_seqlen_k, slot_mapping, None, block_tables)\n        return input_ids, positions\n\n    def prepare_decode(self, seqs: list[Sequence]):\n        input_ids = []\n        positions = []\n        slot_mapping = []\n        context_lens = []\n        for seq in seqs:\n            input_ids.append(seq.last_token)\n            positions.append(len(seq) - 1)\n            context_lens.append(len(seq))\n            slot_mapping.append(seq.block_table[-1] * self.block_size + seq.last_block_num_tokens  - 1)\n        input_ids = torch.tensor(input_ids, dtype=torch.int64, pin_memory=True).cuda(non_blocking=True)\n        positions = torch.tensor(positions, dtype=torch.int64, pin_memory=True).cuda(non_blocking=True)\n        slot_mapping = torch.tensor(slot_mapping, dtype=torch.int32, pin_memory=True).cuda(non_blocking=True)\n        context_lens = torch.tensor(context_lens, dtype=torch.int32, pin_memory=True).cuda(non_blocking=True)\n        block_tables = self.prepare_block_tables(seqs)\n        set_context(False, slot_mapping=slot_mapping, context_lens=context_lens, block_tables=block_tables)\n        return input_ids, positions\n\n    def prepare_sample(self, seqs: list[Sequence]):\n        temperatures = [seq.temperature for seq in seqs]\n        temperatures = torch.tensor(temperatures, dtype=torch.float32, pin_memory=True).cuda(non_blocking=True)\n        return temperatures\n\n    @torch.inference_mode()\n    def run_model(self, input_ids: torch.Tensor, positions: torch.Tensor, is_prefill: bool):\n        if is_prefill or self.enforce_eager or input_ids.size(0) > 512:\n            return self.model.compute_logits(self.model(input_ids, positions))\n        else:\n            bs = input_ids.size(0)\n            context = get_context()\n            graph = self.graphs[next(x for x in self.graph_bs if x >= bs)]\n            graph_vars = self.graph_vars\n            graph_vars[\"input_ids\"][:bs] = input_ids\n            graph_vars[\"positions\"][:bs] = positions\n            graph_vars[\"slot_mapping\"].fill_(-1)\n            graph_vars[\"slot_mapping\"][:bs] = context.slot_mapping\n            graph_vars[\"context_lens\"].zero_()\n            graph_vars[\"context_lens\"][:bs] = context.context_lens\n            graph_vars[\"block_tables\"][:bs, :context.block_tables.size(1)] = context.block_tables\n            graph.replay()\n            return self.model.compute_logits(graph_vars[\"outputs\"][:bs])\n\n    def run(self, seqs: list[Sequence], is_prefill: bool) -> list[int]:\n        input_ids, positions = self.prepare_prefill(seqs) if is_prefill else self.prepare_decode(seqs)\n        temperatures = self.prepare_sample(seqs) if self.rank == 0 else None\n        logits = self.run_model(input_ids, positions, is_prefill)\n        token_ids = self.sampler(logits, temperatures).tolist() if self.rank == 0 else None\n        reset_context()\n        return token_ids\n\n    @torch.inference_mode()\n    def capture_cudagraph(self):\n        config = self.config\n        hf_config = config.hf_config\n        max_bs = min(self.config.max_num_seqs, 512)\n        max_num_blocks = (config.max_model_len + self.block_size - 1) // self.block_size\n        input_ids = torch.zeros(max_bs, dtype=torch.int64)\n        positions = torch.zeros(max_bs, dtype=torch.int64)\n        slot_mapping = torch.zeros(max_bs, dtype=torch.int32)\n        context_lens = torch.zeros(max_bs, dtype=torch.int32)\n        block_tables = torch.zeros(max_bs, max_num_blocks, dtype=torch.int32)\n        outputs = torch.zeros(max_bs, hf_config.hidden_size)\n        self.graph_bs = [1, 2, 4, 8] + list(range(16, max_bs + 1, 16))\n        self.graphs = {}\n        self.graph_pool = None\n\n        for bs in reversed(self.graph_bs):\n            graph = torch.cuda.CUDAGraph()\n            set_context(False, slot_mapping=slot_mapping[:bs], context_lens=context_lens[:bs], block_tables=block_tables[:bs])\n            outputs[:bs] = self.model(input_ids[:bs], positions[:bs])    # warmup\n            with torch.cuda.graph(graph, self.graph_pool):\n                outputs[:bs] = self.model(input_ids[:bs], positions[:bs])    # capture\n            if self.graph_pool is None:\n                self.graph_pool = graph.pool()\n            self.graphs[bs] = graph\n            torch.cuda.synchronize()\n            reset_context()\n\n        self.graph_vars = dict(\n            input_ids=input_ids,\n            positions=positions,\n            slot_mapping=slot_mapping,\n            context_lens=context_lens,\n            block_tables=block_tables,\n            outputs=outputs,\n        )\n",
    "layers/sampler.py": "import torch\nfrom torch import nn\n\n\nclass Sampler(nn.Module):\n\n    @torch.compile\n    def forward(self, logits: torch.Tensor, temperatures: torch.Tensor):\n        logits = logits.float().div_(temperatures.unsqueeze(dim=1))\n        probs = torch.softmax(logits, dim=-1)\n        sample_tokens = probs.div_(torch.empty_like(probs).exponential_(1).clamp_min_(1e-10)).argmax(dim=-1)\n        return sample_tokens\n",
    "engine/scheduler.py": "from collections import deque\n\nfrom nanovllm.config import Config\nfrom nanovllm.engine.sequence import Sequence, SequenceStatus\nfrom nanovllm.engine.block_manager import BlockManager\n\n\nclass Scheduler:\n\n    def __init__(self, config: Config):\n        self.max_num_seqs = config.max_num_seqs\n        self.max_num_batched_tokens = config.max_num_batched_tokens\n        self.eos = config.eos\n        self.block_size = config.kvcache_block_size\n        self.block_manager = BlockManager(config.num_kvcache_blocks, config.kvcache_block_size)\n        self.waiting: deque[Sequence] = deque()\n        self.running: deque[Sequence] = deque()\n\n    def is_finished(self):\n        return not self.waiting and not self.running\n\n    def add(self, seq: Sequence):\n        self.waiting.append(seq)\n\n    def schedule(self) -> tuple[list[Sequence], bool]:\n        scheduled_seqs = []\n        num_batched_tokens = 0\n\n        # prefill\n        while self.waiting and len(scheduled_seqs) < self.max_num_seqs:\n            seq = self.waiting[0]\n            remaining = self.max_num_batched_tokens - num_batched_tokens\n            if remaining == 0:\n                break\n            if not seq.block_table:\n                num_cached_blocks = self.block_manager.can_allocate(seq)\n                if num_cached_blocks == -1:\n                    break\n                num_tokens = seq.num_tokens - num_cached_blocks * self.block_size\n            else:\n                num_tokens = seq.num_tokens - seq.num_cached_tokens\n            if remaining < num_tokens and scheduled_seqs:  # only allow chunked prefill for the first seq\n                break\n            if not seq.block_table:\n                self.block_manager.allocate(seq, num_cached_blocks)\n            seq.num_scheduled_tokens = min(num_tokens, remaining)\n            num_batched_tokens += seq.num_scheduled_tokens\n            if seq.num_cached_tokens + seq.num_scheduled_tokens == seq.num_tokens:\n                seq.status = SequenceStatus.RUNNING\n                self.waiting.popleft()\n                self.running.append(seq)\n            scheduled_seqs.append(seq)\n\n        if scheduled_seqs:\n            return scheduled_seqs, True\n\n        # decode\n        while self.running and len(scheduled_seqs) < self.max_num_seqs:\n            seq = self.running.popleft()\n            while not self.block_manager.can_append(seq):\n                if self.running:\n                    self.preempt(self.running.pop())\n                else:\n                    self.preempt(seq)\n                    break\n            else:\n                seq.num_scheduled_tokens = 1\n                seq.is_prefill = False\n                self.block_manager.may_append(seq)\n                scheduled_seqs.append(seq)\n        assert scheduled_seqs\n        self.running.extendleft(reversed(scheduled_seqs))\n        return scheduled_seqs, False\n\n    def preempt(self, seq: Sequence):\n        seq.status = SequenceStatus.WAITING\n        seq.is_prefill = True\n        self.block_manager.deallocate(seq)\n        self.waiting.appendleft(seq)\n\n    def postprocess(self, seqs: list[Sequence], token_ids: list[int], is_prefill: bool):\n        for seq, token_id in zip(seqs, token_ids):\n            self.block_manager.hash_blocks(seq)\n            seq.num_cached_tokens += seq.num_scheduled_tokens\n            seq.num_scheduled_tokens = 0\n            if is_prefill and seq.num_cached_tokens < seq.num_tokens:\n                continue\n            seq.append_token(token_id)\n            if (not seq.ignore_eos and token_id == self.eos) or seq.num_completion_tokens == seq.max_tokens:\n                seq.status = SequenceStatus.FINISHED\n                self.block_manager.deallocate(seq)\n                self.running.remove(seq)\n",
    "engine/block_manager.py": "from collections import deque\nimport xxhash\nimport numpy as np\n\nfrom nanovllm.engine.sequence import Sequence\n\n\nclass Block:\n\n    def __init__(self, block_id):\n        self.block_id = block_id\n        self.ref_count = 0\n        self.hash = -1\n        self.token_ids = []\n\n    def update(self, hash: int, token_ids: list[int]):\n        self.hash = hash\n        self.token_ids = token_ids\n\n    def reset(self):\n        self.ref_count = 1\n        self.hash = -1\n        self.token_ids = []\n\n\nclass BlockManager:\n\n    def __init__(self, num_blocks: int, block_size: int):\n        self.block_size = block_size\n        self.blocks: list[Block] = [Block(i) for i in range(num_blocks)]\n        self.hash_to_block_id: dict[int, int] = dict()\n        self.free_block_ids: deque[int] = deque(range(num_blocks))\n        self.used_block_ids: set[int] = set()\n\n    @classmethod\n    def compute_hash(cls, token_ids: list[int], prefix: int = -1):\n        h = xxhash.xxh64()\n        if prefix != -1:\n            h.update(prefix.to_bytes(8, \"little\"))\n        h.update(np.array(token_ids).tobytes())\n        return h.intdigest()\n\n    def _allocate_block(self) -> int:\n        block_id = self.free_block_ids.popleft()\n        block = self.blocks[block_id]\n        assert block.ref_count == 0\n        if block.hash != -1 and self.hash_to_block_id.get(block.hash) == block_id:\n            del self.hash_to_block_id[block.hash]\n        block.reset()\n        self.used_block_ids.add(block_id)\n        return block_id\n\n    def _deallocate_block(self, block_id: int):\n        assert self.blocks[block_id].ref_count == 0\n        self.used_block_ids.remove(block_id)\n        self.free_block_ids.append(block_id)\n\n    def can_allocate(self, seq: Sequence) -> int:\n        h = -1\n        num_cached_blocks = 0\n        num_new_blocks = seq.num_blocks\n        for i in range(seq.num_blocks - 1):\n            token_ids = seq.block(i)\n            h = self.compute_hash(token_ids, h)\n            block_id = self.hash_to_block_id.get(h, -1)\n            if block_id == -1 or self.blocks[block_id].token_ids != token_ids:\n                break\n            num_cached_blocks += 1\n            if block_id in self.used_block_ids:\n                num_new_blocks -= 1\n        if len(self.free_block_ids) < num_new_blocks:\n            return -1\n        return num_cached_blocks\n\n    def allocate(self, seq: Sequence, num_cached_blocks: int):\n        assert not seq.block_table\n        h = -1\n        for i in range(num_cached_blocks):\n            token_ids = seq.block(i)\n            h = self.compute_hash(token_ids, h)\n            block_id = self.hash_to_block_id[h]\n            block = self.blocks[block_id]\n            if block_id in self.used_block_ids:\n                block.ref_count += 1\n            else:\n                block.ref_count = 1\n                self.free_block_ids.remove(block_id)\n                self.used_block_ids.add(block_id)\n            seq.block_table.append(block_id)\n        for i in range(num_cached_blocks, seq.num_blocks):\n            seq.block_table.append(self._allocate_block())\n        seq.num_cached_tokens = num_cached_blocks * self.block_size\n\n    def deallocate(self, seq: Sequence):\n        for block_id in reversed(seq.block_table):\n            block = self.blocks[block_id]\n            block.ref_count -= 1\n            if block.ref_count == 0:\n                self._deallocate_block(block_id)\n        seq.num_cached_tokens = 0\n        seq.block_table.clear()\n\n    def can_append(self, seq: Sequence) -> bool:\n        return len(self.free_block_ids) >= (len(seq) % self.block_size == 1)\n\n    def may_append(self, seq: Sequence):\n        if len(seq) % self.block_size == 1:\n            seq.block_table.append(self._allocate_block())\n\n    def hash_blocks(self, seq: Sequence):\n        start = seq.num_cached_tokens // self.block_size\n        end = (seq.num_cached_tokens + seq.num_scheduled_tokens) // self.block_size\n        if start == end: return\n        h = self.blocks[seq.block_table[start - 1]].hash if start > 0 else -1\n        for i in range(start, end):\n            block = self.blocks[seq.block_table[i]]\n            token_ids = seq.block(i)\n            h = self.compute_hash(token_ids, h)\n            block.update(h, token_ids)\n            self.hash_to_block_id[h] = block.block_id\n"
  }
}